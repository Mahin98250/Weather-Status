async function getWeather(){const city=document.getElementById('cityInput').value;const geo=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${city}&count=1`).then(r=>r.json());if(!geo.results){alert('City not found');return;}const {latitude,longitude,name,country}=geo.results[0];const data=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,wind_speed_10m,weather_code&daily=temperature_2m_max,temperature_2m_min&timezone=auto`).then(r=>r.json());document.getElementById('weather').innerHTML=`<div class='card'><h2>${name}, ${country}</h2><h3>${data.current.temperature_2m}°C</h3><p>Wind: ${data.current.wind_speed_10m} km/h</p></div>`;let days='';for(let i=0;i<7;i++){days+=`<p>${data.daily.time[i]} - ${data.daily.temperature_2m_max[i]}° / ${data.daily.temperature_2m_min[i]}°</p>`;}document.getElementById('forecast').innerHTML=`<div class='card'><h3>7-Day Forecast</h3>${days}</div>`;}